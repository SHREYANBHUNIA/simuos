# Metrics contracts

The metrics layer reports waiting time, turnaround time, response time, throughput, CPU utilization, and algorithm-level aggregates. The values are derived from immutable timeline segments rather than maintained separately.
