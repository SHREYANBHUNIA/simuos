# Simulation orchestration

Simulation orchestration records the selected algorithm, parameter set, workload, final timeline, and aggregate metrics. This object is the unit persisted by the FastAPI service and compared in the React laboratory.
