# Test strategy

The browser simulation engine is covered by Vitest in `client/src/lib/simulations.test.ts`. Equivalent Rust and FastAPI tests should use the same deterministic workload fixtures when those modules are connected to deployment.

