# Frontend implementation

The React and TypeScript application source lives in `client/` to respect the managed web build. The UI provides the interactive scheduling, page-replacement, allocation, and comparison laboratories.
